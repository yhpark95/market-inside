<!DOCTYPE html>
<html lang="">
<head>
    <title>Total Shipment Information</title>
    <link href="../static/style.css" rel="stylesheet" type="text/css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
        }

        h1 {
            text-align: center;
            background-color: #007bff;
            color: #fff;
            padding: 20px;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        .shipment-info {
            text-align: center;
            margin-top: 20px;
            padding: 10px;
            background-color: #f9f9f9;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        .button-container {
            text-align: center;
            margin-top: 20px;
        }

        button {
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
<h1>Total Shipment Information</h1>

<div class="container">
    <!-- Display Total Shipment Number -->
    {{#response}}
        <div class="shipment-info">
            <p>Total Shipment Number: {{totalShipmentValue}}</p>
        </div>
    {{/response}}

    <!-- Buttons to Download Data or Go Back -->
    <div class="button-container">
        <form id="downloadData">
            {{#request}}
                <button type="submit">Download All Data</button>
                <input id="fromDate" name="fromDate" pattern="\d{4}-\d{2}-\d{2}" required type="date"
                       value="{{fromDate}}"
                       hidden="hidden">
                {{#response}}
                    <input id="totalShipmentValue" name="totalShipmentValue" required type="text"
                           value="{{totalShipmentValue}}"
                           hidden="hidden">
                {{/response}}
                <input id="toDate" name="toDate" pattern="\d{4}-\d{2}-\d{2}" required type="date" value="{{toDate}}"
                       hidden="hidden">
                <input id="hsCode" name="hsCode" required type="text" value="{{hsCode}}" hidden="hidden">
                <input id="country" name="country" required type="text" value="{{country}}" hidden="hidden">
                <input id="dataTypeId" name="dataTypeId" required type="text" value="{{dataTypeId}}"
                       hidden="hidden">
                <input id="importExport" name="importExport" required type="text" value="{{importExport}}"
                       hidden="hidden">
            {{/request}}
        </form>
        <form action="/" method="get">
            <button type="submit">Go Back to Condition Selection</button>
        </form>
    </div>
</div>
</body>
</html>
